import styled from 'styled-components';

export const StyleCard = styled.div`
    width: auto;
    height: auto;
    display: flex;
    justify-content: center;
    img{
        width: 75%;
        height: auto;
    }
`;